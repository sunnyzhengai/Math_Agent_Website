services:
  - type: web
    name: math-agent-poc
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api/server.py
    envVars:
      - key: PORT
        value: 10000
