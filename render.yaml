services:
  - type: web
    name: math-agent-poc
    env: python
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker api.server:app --bind 0.0.0.0:$PORT --timeout 90
    envVars:
      - key: PORT
        value: 10000
