# Telemetry configuration for Math Agent
#
# TELEMETRY_ENABLED: Enable/disable event logging (default: true)
TELEMETRY_ENABLED=true

# TELEMETRY_PATH: JSONL file path for event logs (default: logs/telemetry.jsonl)
TELEMETRY_PATH=logs/telemetry.jsonl

# TELEMETRY_MAX_BYTES: Log file size threshold before rotation (default: 5242880 = 5MB)
# Production recommended: 10485760 (10MB) or higher
# Local dev: 5242880 (5MB)
TELEMETRY_MAX_BYTES=5242880

# TELEMETRY_SAMPLE_RATE: Fraction of events to log, 0.0-1.0 (default: 1.0 = all)
# Production recommended: 0.1 (10% sampling) to reduce disk usage
# Local dev: 1.0 (log everything)
TELEMETRY_SAMPLE_RATE=1.0

# SERVER_ID: Identifier for this server (for log aggregation)
SERVER_ID=local-dev

# APP_VERSION: Application version (for schema tracking)
APP_VERSION=0.1.0
