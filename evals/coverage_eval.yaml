name: template_coverage
description: Verify all question templates get used over time
thresholds:
  min_coverage_50_questions: 0.8  # 80% of templates should appear in 50 questions
  min_coverage_100_questions: 0.95  # 95% of templates should appear in 100 questions
  max_questions_to_full_coverage: 50  # Should see all templates within 50 questions
